<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Table</title>
</head>
<body>
    
    <table>
        <thead>
            <% header.forEach(col => { %>
                <th><%= col %></th>
            <% }) %>
        </thead>

        <tbody>
            <% rows.forEach(row => { %>
                <tr>
                    <td><%= row[0] %></td>
                    <td><%= row[1] %></td>
                    <td><%= row[2] %></td>
                </tr>
            <% }) %>
        </tbody>
    </table>
</body>
</html>